{
  "name": "@blinkered/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capacitor shell. Consumes apps/web's build output; owns no game code.",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "build-number": "node tools/bump-build.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
