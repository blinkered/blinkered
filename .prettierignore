# Generated by pnpm and rewritten on every install.
pnpm-lock.yaml
coverage/
dist/
dist-tsc/

# Generated by `pnpm dictionary build`, and the vendored licence texts must stay verbatim.
packages/words/data/

# Xcode owns these and rewrites them in its own style; reformatting only starts an argument.
apps/mobile/ios/

# Helm templates are Go templates wearing a .yaml suffix, so a YAML parser cannot read them.
# `helm lint` and `helm template` are what check these; see docs/DEPLOY.md.
deploy/helm/*/templates/

# Written by `drizzle-kit generate` from src/schema.ts. The SQL is the migration and the snapshot
# is how the next diff is computed; reformatting either only invites a conflict with the tool.
apps/server/drizzle/
